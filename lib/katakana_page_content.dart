import 'package:flutter/material.dart';

class KatakanaPageContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Content for Katakana',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
